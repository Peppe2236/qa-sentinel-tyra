Copy the `tests` and `utils` folders into:

C:\Users\Pette\nation-playwright-tests

Replace the existing `tests\homepage.spec.ts`.

Run:
npx playwright test tests/homepage.spec.ts --headed

Visual UI:
npx playwright test tests/homepage.spec.ts --ui

Report:
npx playwright show-report
